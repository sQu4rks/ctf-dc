config = {}

# Intersight config
config['INTERSIGHT_API_KEY'] = "<the intersight api key>"
config['INTERSIGHT_CERT'] = "cert.pem"

# ACI config
# Credentials below are for the always-on sandbox
config['ACI_BASE_URL'] = "https://sandboxapicdc.cisco.com"
config['ACI_USER'] = "admin"
config['ACI_PASSWORD'] = "ciscopsdt"